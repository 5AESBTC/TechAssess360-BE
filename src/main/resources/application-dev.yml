server:
  port: 8181
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/tech_rate?useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false
    username: root
    password: Sol8ight@123./
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true